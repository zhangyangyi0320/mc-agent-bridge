# 免责声明 / Disclaimer

本项目（McAgentBridge）以 MIT 许可证开源，仅供学习与研究使用。

**使用本插件产生的一切后果均由使用者自行承担。** 本插件通过 HTTP 接口对 Minecraft 服务器执行强权限操作，包括但不限于：执行服务器命令、读写服务器文件、启停服务、管理玩家与插件等。作者不对因使用本插件而导致的任何后果负责，包括但不限于：

- 服务器数据丢失、损坏或被破坏；
- 服务器被未授权访问、被入侵或用于任何形式的滥用；
- 玩家数据、账号或世界存档受损；
- 任何直接、间接的经济损失或其他损害。

使用者有责任：

1. 自行评估并承担全部使用风险；
2. 在生产环境使用前进行充分测试；
3. 妥善配置 `token` 与 `host`，仅在可信网络（如 `127.0.0.1`）暴露接口，并建议使用反向代理与 TLS 保护通信；
4. 妥善保管访问令牌，避免泄露。

**使用该插件的全部后果均由使用者本人承担，作者不承担任何责任。**
